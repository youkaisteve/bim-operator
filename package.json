{
  "name": "bim-operator",
  "version": "1.0.2",
  "description": "a pack of bim operations",
  "files": [
    "dist"
  ],
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rm -fr ./dist && tsc -p tsconfig.dev.json",
    "build": "rm -fr ./dist && tsc -p tsconfig.prod.json && cp package.json ./dist/",
    "pub": "npm run build && git commit -am upgrade && git push && npm version patch && git push && npm publish"
  },
  "devDependencies": {
    "@types/node": "^10.5.5",
    "typescript": "^3.5.0",
    "ts-node": "^8.3.0",
    "tslib": "^1.8.1",
    "tslint": "^5.11.0",
    "tslint-midway-contrib": "1"
  },
  "keywords": [
    "bim"
  ],
  "repository": {
    "type": "git",
    "url": "git@gitee.com:youkaisteve/bim-operator.git"
  },
  "author": "steve.k.you",
  "license": "MIT"
}
